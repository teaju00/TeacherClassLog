1. 아키텍처 및 기술 스택
프론트엔드 프레임워크 금지: React, Vue, Svelte 등의 프레임워크나 빌드 도구를 절대 사용하지 마라.

Vanilla 스택 사용: 오직 HTML5 시맨틱 마크업, Vanilla JavaScript(ES6+), 그리고 Tailwind CSS 3.4.x CDN만 사용하라.

단일 파일 원칙: 초기 프로토타입은 반드시 index.html 단일 파일 내에 HTML, CSS, JS 로직을 모두 포함하여 작성하라.

백엔드 연동: Firebase v10 이상의 CDN 모듈(Authentication, Firestore)을 사용하라. 외부 라이브러리(jQuery 등)는 사용을 금지하며, 차트가 필요할 때만 Chart.js를 허용한다.

2. UI/UX 및 디자인 시스템 강제
언어 정책: 애플리케이션 내의 모든 UI 텍스트, 버튼 라벨, 안내 메시지는 100% 한국어로만 작성하라. 영어 단어 노출을 금지한다.

이모지 금지: UI 상에 이모지(Emoji) 사용을 절대 금지한다. 아이콘이 필요한 곳에는 Heroicons 기반의 인라인 SVG 태그를 사용하라.

모바일 최적화 (한 손 조작): 화면 상단 네비게이션을 피하고, 스마트폰 하단에 고정된 바텀 네비게이션(Bottom Navigation)과 우측 하단 플로팅 액션 버튼(FAB)을 사용하여 엄지손가락만으로 조작 가능하게 설계하라.

빠른 데이터 입력 (점진적 공개):

학생 카드 클릭 시 커다란 입력창이 아닌, 하단에서 위로 올라오는 바텀 시트(Bottom Sheet) 모달을 띄워라.

태그 입력은 다중 선택 칩(Multi-select Chips) 형태로 구현하되, 터치 오류를 막기 위해 칩의 터치 영역을 최소 48x48px 크기로 확보하라.

3. 데이터베이스(Firestore) 및 성능 최적화
서버 사이드 집계: 클라이언트에서 모든 태그 데이터를 불러와서 주의/경고 학생 수를 계산하지 마라. 읽기 비용 절감과 속도 향상을 위해 반드시 Firestore의 내장 집계 쿼리인 count() 함수를 사용하라.

데이터 역정규화(Denormalization): 특정 학생의 누적 태그 횟수나 경고 횟수는 매번 계산하지 마라. 교사가 태그를 추가할 때 데이터베이스 트랜잭션을 통해 students 컬렉션의 학생 문서 내부 필드(warningCount 등)를 증가(increment)시켜 실시간 조회 성능을 높여라.

방어 로직 및 보안:

Firestore 데이터를 읽거나 쓸 때는 반드시 Firebase Authentication 객체가 초기화되어 있고 사용자가 로그인 상태인지 먼저 검증하라.

firestore.rules 작성 시, 모든 데이터(students, tags, observations)는 반드시 생성한 교사의 ID(teacherId 또는 request.auth.uid)와 일치할 때만 접근할 수 있도록 보안 규칙을 철저히 설정하라.

4. 코딩 컨벤션
코드 분리 및 주석: index.html 파일이 길어지더라도 길을 잃지 않도록 ``, // Firebase 초기화, // 데이터 로직, // UI 업데이트 로직 등 주석을 통해 논리적 구역을 명확히 분리하라.

JSDoc 작성: 모든 함수는 단일 책임 원칙(SRP)을 지키고, 상단에 JSDoc 스타일로 한글 주석을 달아 파라미터와 반환값을 상세히 설명하라.