# 학급 관찰 일지 시스템: 단계별 개발 페이즈 (Phased Development Plan)

본 문서는 `implementation_plan.md`에 명시된 아키텍처와 기능을 어떠한 순서와 전략으로 개발할지 구체적으로 나눈 단계별 로드맵입니다. Netlify 배포에 따른 네트워크 지연 시간을 극복하기 위한 **낙관적 UI(Optimistic UI)** 적용을 핵심 개발 전략으로 삼습니다.

👉 [시스템 구현 계획서(Implementation Plan) 확인하기](file:///C:/Users/USER/.gemini/antigravity/brain/6a36f00f-78a8-446d-8187-5210c949f250/implementation_plan.md)

---

## Phase 1: 기반 아키텍처 및 코어 UI 컴포넌트 구축
프론트엔드의 시각적 뼈대를 구축하고 파이어베이스 인프라를 연결하는 단계입니다.

- **목표**: 하드코딩된 더미 데이터를 사용해 시나리오 기반의 정적 UI 완성 및 인증 기초 연동
- **주요 작업 내용**:
  1. `index.html` 단일 파일 생성 및 Tailwind CSS CDN 설정 (Stitch Custom Theme 매핑).
  2. Firebase v10 CDN 스크립트 로드 및 `Teacher Class Log` 프로젝트 `firebaseConfig` 연결 (일단 구조화 작업).
  3. 로그인 UI 화면 구성 및 **구글 로그인(Google Auth)** 팝업 연동 로직 적용.
  4. 메인 학생 목록 뷰 (Fluid Grid) 레이아웃 마크업 완성.
  5. 하단 네비게이션(Bottom Nav) 및 우측 플로팅 액션 버튼(FAB) 제작.
  6. 다중 선택 칩(Multi-select Chip)의 컴포넌트 단위 스타일링(상태별 색상, 터치 영역 48px).

---

## Phase 2: 시나리오 A - 마이크로 세션 관찰 일지 입력
당장 수업 직후 짧게 기록할 수 있는 핵심 코어 기능을 연동합니다.

- **목표**: 10초 이내에 학생 태그를 입력하고 Firestore에 비동기로 전송하는 기능 완성.
- **주요 작업 내용**:
  1. 바텀 시트(Bottom Sheet) 모달 UI 및 애니메이션 슬라이딩 동작 구현.
  2. 학생 데이터를 실제 Firestore에서 불러와 메인 화면에 렌더링.
  3. **[네트워크 지연 극복 핵심] 낙관적 UI 업데이트 (Optimistic UI)** 적용:
     - 교사가 '저장'을 누르면 DB 응답을 기다리지 않고 즉각 시트를 닫고 성공한 것처럼 UI를 선 업데이트합니다.
     - 네트워크 백그라운드에서 `observations` 컬렉션에 문서를 추가하고, `students` 컬렉션의 누적 데이터를 증가(increment)시킵니다.
  4. '메모 추가' 버튼 클릭 시 텍스트 영역(Textarea) 및 상용구 칩 클릭 시 텍스트 자동 삽입 로직 구현.

---

## Phase 3: 시나리오 B & C - 마법사 뷰 및 상담 대시보드
입력이 완료된 데이터를 효율적으로 소비하고, 누락된 학생 데이터를 채워넣는 뷰를 완성합니다.

- **목표**: 무마찰(Frictionless) 연속 입력 플로우 및 데이터 시각화의 완성
- **주요 작업 내용**:
  1. (시나리오 B) 플로팅 액션 버튼(FAB) 클릭 시 아직 평가되지 않은 학생 리스트 필터링 로직 구현 (진행 상태 바 연동).
  2. (시나리오 B) '저장 후 다음 학생' 로직: 낙관적 UI를 활용해 DB 트랜잭션 전송 후 즉각 카드를 좌측으로 밀어내고 다음 학생 폼으로 자동 전환하는 트랜지션 로직.
  3. (시나리오 C) 메인 리스트에서 학생 카드 클릭 시 상세 페이지(대시보드 모달)로 진입.
  4. (시나리오 C) 누적 태그 수를 뱃지 형태로 시각화(경고/산만함 등의 부정 태그는 빨간색 계열로 강조).
  5. (시나리오 C) 최근 메모 기록 타임라인 역순 렌더링.

---

## Phase 4: 폴리싱 및 Netlify 배포
사용성 테스트와 성능 최적화를 수행한 뒤 프로덕션으로 이관합니다.

- **목표**: QA 및 실 서비스 배포.
- **주요 작업 내용**:
  1. 3G 쓰로틀링 환경(네트워크 지연 상태)에서 낙관적 UI가 어색함 없이 잘 동작하는지 시뮬레이션.
  2. Firestore Security Rules(`firestore.rules`) 확립: 각 데이터가 로그인된 구글 이메일의 UID에 종속되는지 격리 테스트.
  3. 콘솔 에러 정리 및 렌더링 성능(Repaint 최소화) 최적화.
  4. Netlify를 통한 수동/자동 배포 구성 및 최종 프로덕션 점검.
